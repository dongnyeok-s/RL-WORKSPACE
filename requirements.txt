# RL Workspace 통합 의존성
# 설치: pip install -r requirements.txt

# 강화학습 환경
gymnasium[classic-control]==1.2.3
gymnasium[box2d]==1.2.3       # LunarLander (brew install swig 필요)

# 시각화
pygame==2.6.1
matplotlib>=3.8.0
tensorboard>=2.15.0

# 딥러닝 (Anaconda에 이미 설치되어 있다면 아래 주석 해제 불필요)
# torch>=2.0.0
# numpy>=1.24.0
